# Third-party reference clones (studied, not vendored)
/reference/
# ggml source clone (fetched via scripts/setup.sh; treat as external dep)
/vendor/

# Build artifacts
/build/
build_configure.log
*.o
*.a
*.gguf

# Models / audio
/models/
*.safetensors
*.wav

# Conversation log (per global convention)
Conversation.md
